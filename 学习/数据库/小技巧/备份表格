1.复制表结构和数据
CREATE TABLE TABLE__NAME_COPY AS SELECT * FROM TABLE_NAME
2.只复制表结构，不复制数据
CREATE TABLE TABLE__NAME_COPY AS SELECT * FROM TABLE_NAME WHERE 1=2
或者CREATE TABLE TABLE__NAME_COPY LIKE TABLE_NAME
3.只复制数据
两个表结构一样
INSERT INTO TABLE_NAME_COPY SELECT * FROM TABLE_NAME
两个表结构不一样
INSERT INTO TABLE_NAME_COPY(col1,col2,...) SELECT col1,col2,.. FROM TABLE_NAME
